version: '3.3'

services:

  # ------------------------------------
  # App Container
  # ------------------------------------

  podio_api-postgres:
    container_name: podio_api-postgres
    deploy:
      resources:
        reservations:
          cpus: '0.5'
          memory: 1000M
        limits:
          cpus: '1'
          memory: 1000M
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    image: gbvsilva/podio-api:postgres
    env_file:
      - "${PODIO_API_ENVFILE}"
      - "${DATABASE_ENVFILE}"
      - "${MISC_ENVFILE}"
      - "${TELEGRAM_ENVFILE}"
